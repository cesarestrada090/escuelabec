# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.spec.ts >> Auth - Login >> muestra error con email inexistente
- Location: e2e/tests/auth.spec.ts:29:7

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: locator('.alert-danger')
Expected substring: "Usuario no encontrado"
Received string:    "Credenciales inválidas. Verifica tu email y contraseña."
Timeout: 10000ms

Call log:
  - Expect "toContainText" with timeout 10000ms
  - waiting for locator('.alert-danger')
    24 × locator resolved to <div class="alert alert-danger" _ngcontent-ng-c2744582434="">Credenciales inválidas. Verifica tu email y contr…</div>
       - unexpected value "Credenciales inválidas. Verifica tu email y contraseña."

```

```yaml
- text: Credenciales inválidas. Verifica tu email y contraseña.
```

# Test source

```ts
  1  | import { Page, Locator, expect } from '@playwright/test';
  2  | 
  3  | export class LoginPage {
  4  |   readonly page: Page;
  5  |   readonly emailInput: Locator;
  6  |   readonly passwordInput: Locator;
  7  |   readonly submitButton: Locator;
  8  |   readonly errorAlert: Locator;
  9  | 
  10 |   constructor(page: Page) {
  11 |     this.page = page;
  12 |     this.emailInput = page.locator('input[type="email"], input[name="email"]');
  13 |     this.passwordInput = page.locator('input[type="password"], input[name="password"]');
  14 |     this.submitButton = page.locator('button[type="submit"]');
  15 |     this.errorAlert = page.locator('.alert-danger');
  16 |   }
  17 | 
  18 |   async goto() {
  19 |     await this.page.goto('/auth/login');
  20 |     await this.page.waitForLoadState('networkidle');
  21 |   }
  22 | 
  23 |   async login(email: string, password: string) {
  24 |     await this.emailInput.fill(email);
  25 |     await this.passwordInput.fill(password);
  26 |     await this.submitButton.click();
  27 |   }
  28 | 
  29 |   async loginAndWait(email: string, password: string) {
  30 |     await this.login(email, password);
  31 |     await this.page.waitForURL(/\/dashboard/, { timeout: 15000 });
  32 |   }
  33 | 
  34 |   async expectError(text: string) {
  35 |     await expect(this.errorAlert).toBeVisible();
> 36 |     await expect(this.errorAlert).toContainText(text);
     |                                   ^ Error: expect(locator).toContainText(expected) failed
  37 |   }
  38 | 
  39 |   async expectRedirectToDashboard() {
  40 |     await expect(this.page).toHaveURL(/\/dashboard/, { timeout: 15000 });
  41 |   }
  42 | }
  43 | 
```